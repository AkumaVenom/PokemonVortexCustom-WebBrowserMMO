/* Pokémon Vortex team-page compatibility shim.
   The recovered change-team page posts directly to PHP; no additional client
   controller is required in the reconstructed build. This file intentionally
   exists so the historic script reference resolves without a 404. */
(function(){ 'use strict'; })();
